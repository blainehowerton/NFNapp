<h1>Articles</h1>

We currently have <%= Article.public_count %> articles and counting!

<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
      <li>
        <%= link_to article.title, article %>
      </li>
    <% end %>
  <% end %>
</ul>

<h3><%= link_to "New Article", new_article_path %></h3>

<%= link_to "Home", home_index_path %>  |  <%= link_to "Contacts", contacts_path %> 
 
